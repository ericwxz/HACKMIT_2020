import data

def InitializeUser(username, password, contactInfo, location):
    """Checks if location is initialized; adds to location tree, adds user to database, returns -1 on failure"""
    
    return -1

def DisplayUserProfile(username):
    """returns a json"""